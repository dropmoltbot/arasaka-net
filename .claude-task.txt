You are a senior Babylon.js engineer. Execute this task in arasaka-net:

1. Read: src/lib/constants.ts
2. Add to COLORS export: neonPurple: "#bf5af2"
3. Read: src/components/Scene/Scene.tsx
4. Improve the ambient drone sound in Scene.tsx: make it layered (add a second oscillator at 110Hz, slightly louder than the first, with its own LFO at 0.3Hz). Make both oscillators ramp up to 1.5x intensity when an "attack wave" fires (call window.dispatchEvent(new Event('attack-wave')) every 90 seconds).
5. Read: src/components/UI/Header.tsx and add a small animated "signal bars" indicator (4 bars, CSS animation) next to the status dot.
6. Run: npm run build in /home/node/.openclaw/workspace/arasaka-net
7. Fix any TypeScript errors. Commit changes with message: "feat: layered drone + signal bars + neonPurple". Push to origin.

After each step, report what you did in one line. If a step fails, say ERROR and stop.

When done, exit with the word COMPLETE.
